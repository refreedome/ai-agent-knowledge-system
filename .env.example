# ─────────────────────────────────────────────
# 环境变量模板（复制为 .env 并填写真实值）
# 用法: cp .env.example .env   （Windows: copy .env.example .env）
# .env 已被 .gitignore 忽略，密钥绝不提交到 Git！
# ─────────────────────────────────────────────

# ── 必填:大模型 API 密钥(二选一,由 config/rag.yml 的 provider 决定) ──
# 默认 provider=dashscope(通义千问),填 DASHSCOPE_API_KEY
# 获取地址: https://bailian.console.aliyun.com/
DASHSCOPE_API_KEY=sk-your-dashscope-api-key

# 若把 config/rag.yml 的 chat_provider 改成 deepseek,则填这一项(可两项都填,按配置生效)
# 获取地址: https://platform.deepseek.com/api_keys
# DEEPSEEK_API_KEY=sk-your-deepseek-api-key

# ── 可选:CORS 允许来源(逗号分隔) ──
# 生产环境务必收紧为你的真实域名/IP,例如:
# ALLOWED_ORIGINS=https://api.yourdomain.com,http://your-ip:8501
# 不设置则默认仅允许本机 (localhost:8501/8000)
# ALLOWED_ORIGINS=http://localhost:8501,http://127.0.0.1:8501
